#!/bin/sh

mount -t tmpfs tmpfs /tmp
mount -t proc proc /proc
mount -t sysfs sys /sys
#devtmpfs is being mounted automatically
#mount -t devtmpfs none /dev

echo "initramfs: mount"

/bin/mount /new_root

exec /sbin/switch_root /new_root /sbin/init
